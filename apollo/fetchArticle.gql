query ($id: Int!){
  story(id: $id) {
    id
    descendants
    webpage {
      article {
        id
        author
        headline
        published_at
        html
        main_image
        main_image_unique
        canonical_url
      }
    }
  }
}
